name: Daily Sync from Hugging Face Dataset

on:
  schedule:
    - cron: '0 0 * * *'  # Runs daily at midnight UTC (which is 8 AM Beijing time)
  workflow_dispatch:      # Allows manual trigger

jobs:
  sync-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          lfs: true # Enable LFS support
          fetch-depth: 0

      - name: Global Config
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git lfs install

      - name: Sync Data
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          # 1. Add upstream repository (source)
          git remote add upstream https://huggingface.co/datasets/bwzheng2010/yahoo-finance-data

          # 2. Add your own repository as target
          git remote add target https://${{ github.actor }}:${{ secrets.HF_TOKEN }}@huggingface.co/datasets/${{ github.repository_owner }}/yahoo-finance-data

          # 3. Fetch latest data from upstream
          git fetch upstream main

          # 4. Merge data, preferring upstream in case of conflicts
          git merge upstream/main -m "Auto-sync from bwzheng2010 [skip ci]" -X theirs

          # 5. Push merged data to your GitHub repository
          git push origin main

          # 6. Push to your Hugging Face dataset (optional - only if you have it created)
          # Uncomment the next line if you want to push to your Hugging Face dataset
          # git push target main --force